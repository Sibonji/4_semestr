#ifndef ENUM_H
#define ENUM_H

enum ret_names{
    success,
    found,

    zero_login,
    zero_pointer,
    bad_size,
    elem_exist,
    bad_alloc,
    not_exist,

    undef_cmd_name,

    valid,
    not_valid,
    test_fail,
    test_pass
};

#endif